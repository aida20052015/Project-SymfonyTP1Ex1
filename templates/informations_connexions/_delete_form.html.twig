<form method="post" action="{{ path('app_informations_connexions_delete', {'id': informations_connexion.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ informations_connexion.id) }}">
    <button class="btn">Delete</button>
</form>
